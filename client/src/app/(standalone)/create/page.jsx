import CreateCommunityWizard from "@/components/create/CreateCommunityWizard";

export const metadata = {
  title: "Create a Community — Skhooler",
};

export default function CreatePage() {
  return <CreateCommunityWizard />;
}
